mixin video(info)
    a(href=`/videos/${info.id}`).video-mixin
        div.video-mixin__thumb
        div.video-mixin__data
            span.video-mixin__title=info.title
            .video-mixin__meta 
                span #{info.owner.userid} ● 
                span #{info.meta.views} 회
           
    
        