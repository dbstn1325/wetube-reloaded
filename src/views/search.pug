extends base.pug
include mixins/video.pug

block content
    form(method="POST").search__form
        input(placeholder="Search Video", type="text", name="keyword")
        button
            i.fas.fa-search
            
    div.video-grid
        each video in videos
            +video(video)
        else 
            span.empty__message No video found