extends base.pug

block content
    
    div
        p=video.description
        small=video.createdAt 
        
    a(href=`${video.id}/edit`) Edit Video &rrar;

    h4 #{video.views} #{video.views === 1 ? "view":"views"}