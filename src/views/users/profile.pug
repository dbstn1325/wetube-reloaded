extends ../base.pug
include ../mixins/video

block content
    div.profile__data
        img(src="/" + user.avatarUrl).avatar
        h3=user.name
    div.video-grid
        each item in foundUser.videos
            +video(item)
        else 
            span.empty__message No video found
        